/* const cartReducer = (state = null, action) => {
  switch (action.type) {
    case "ADMIN_LOGIN":
      return action.payload;
    default:
      return state;
  }
};

export default cartReducer;
 */
